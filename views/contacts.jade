extends layout

block content
  div(class='container')
    h1 Welcome #{username}
    ul(data-role='listview')
      each address in addresses
        li
          a(href='/contact_info?id=#{address._id}') #{address.name}
          
  a(href='/add_address') Add new contact